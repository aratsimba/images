
# ROCA — Religious Organization Calendar Application
## Implementation Tasks

### Sprint 1: Foundation (Weeks 1-2)

- [ ] **Task 1: Project Scaffold & Monorepo Setup**
  - Initialize monorepo with Turborepo/Nx
  - Create apps/web (React 18 + Vite + TypeScript + Tailwind)
  - Create apps/mobile (React Native + Expo SDK 50)
  - Create apps/api (Node.js + Express + TypeScript)
  - Create packages/shared (types, constants, utils)
  - Create packages/ui (shared component library)
  - Create packages/config (ESLint, Prettier, TSConfig)
  - Configure path aliases and workspace dependencies
  - **Dependencies:** None
  - **Acceptance:** `pnpm dev` starts all three apps; `pnpm lint` + `pnpm typecheck` pass

- [ ] **Task 2: Database Schema & Migrations**
  - Install Prisma; configure PostgreSQL connection
  - Create migration 001: organizations, campuses, rooms tables
  - Create migration 002: members table with role enum
  - Create migration 003: events, event_categories tables with RRULE column
  - Create migration 004: event_registrations table with status enum
  - Create migration 005: liturgical_seasons, scripture_readings, prayer_focuses
  - Create migration 006: ministry_groups, group_members
  - Create migration 007: volunteer_roles, volunteer_assignments
  - Create migration 008: notifications table
  - Create all 8 indexes (idx_events_org_date, idx_events_category, etc.)
  - Verify all foreign keys and constraints
  - **Dependencies:** Task 1
  - **Acceptance:** `prisma migrate deploy` succeeds; all 15 tables created with correct relationships

- [ ] **Task 3: Seed Data Script**
  - Create seed for sample organization (Grace Community Church)
  - Seed 2 campuses (Main Campus, North Campus)
  - Seed 5 rooms with capacity and accessibility flags
  - Seed 20 members across all 7 roles
  - Seed 6 event categories with colors (Worship, Bible Study, Women's Ministry, Admin, Staff, Outreach)
  - Seed liturgical seasons for 2026 (Ordinary Time, Advent, Christmas, Lent, Easter)
  - Seed 10 recurring events (Sunday worship ×2, staff meeting, Bible studies, etc.)
  - Seed scripture readings for August 2026
  - Seed volunteer roles (Greeter, Usher, Sound Tech, Worship Lead, Nursery, Welcome Desk)
  - **Dependencies:** Task 2
  - **Acceptance:** Seed runs without errors; all relationships valid; sample queries return expected data

- [ ] **Task 4: Authentication System**
  - Implement POST /api/v1/auth/register (bcrypt hash, create member, return JWT pair)
  - Implement POST /api/v1/auth/login (credential validation, issue tokens)
  - Implement POST /api/v1/auth/refresh (validate refresh token, issue new pair)
  - Implement POST /api/v1/auth/logout (revoke refresh token)
  - Implement JWT middleware (verify RS256 signature, extract org_id + role)
  - Implement RBAC middleware (role hierarchy check against required roles array)
  - Create Zod schemas for all auth request bodies
  - Write unit tests for auth service (≥90% coverage)
  - **Dependencies:** Task 2
  - **Acceptance:** Full auth lifecycle works; expired tokens return 401; insufficient roles return 403

- [ ] **Task 5: CI/CD Pipeline**
  - Configure GitHub Actions workflow for PR checks (lint, typecheck, unit tests)
  - Configure build step for API Docker image
  - Configure staging deployment to ECS (blue/green)
  - Add PostgreSQL service container for integration tests
  - Configure code coverage reporting (≥80% gate)
  - Add security scanning (npm audit, Snyk)
  - **Dependencies:** Task 1
  - **Acceptance:** PRs auto-run checks; merge to main triggers staging deploy

- [ ] **Task 6: Docker & Local Development**
  - Create Dockerfile for API (multi-stage build)
  - Create docker-compose.yml (PostgreSQL 15, Redis 7, MinIO for S3, API)
  - Create .env.example with all required variables
  - Document local setup in README.md
  - **Dependencies:** Task 1
  - **Acceptance:** `docker-compose up` starts full local stack; API connects to DB and Redis

---

### Sprint 2: Core Events & Calendar (Weeks 3-4)

- [ ] **Task 7: Events CRUD API**
  - Implement POST /api/v1/events (create with validation, room conflict check)
  - Implement GET /api/v1/events/:id (with category, campus, room joins)
  - Implement PUT /api/v1/events/:id (update with conflict re-check)
  - Implement DELETE /api/v1/events/:id (soft delete → status: cancelled)
  - Implement POST /api/v1/events/:id/duplicate (clone event)
  - Implement visibility filtering based on requester role
  - Create Zod validation schemas for event create/update
  - Write integration tests for all event endpoints
  - **Dependencies:** Task 4
  - **Acceptance:** All CRUD operations work; room conflicts return EVENT_002; visibility enforced

- [ ] **Task 8: Recurrence Engine**
  - Integrate rrule.js for RRULE parsing and expansion
  - Implement calendar query with recurrence expansion (GET /events?start=&end=)
  - Handle recurrence exception cases (EXDATE for cancelled instances)
  - Implement "Edit this instance / This and future / All" scope logic
  - Implement series splitting for "this and future" edits
  - Write unit tests for RRULE expansion edge cases (DST, end-of-month, leap year)
  - **Dependencies:** Task 7
  - **Acceptance:** Weekly Sunday worship expands correctly across date ranges; exceptions work

- [ ] **Task 9: Event Categories & Campus Filtering**
  - Implement CRUD for /api/v1/event-categories (color_hex, icon, org-scoped)
  - Implement campus CRUD /api/v1/campuses
  - Add campus + category filter params to GET /events
  - Implement rooms CRUD with availability check endpoint
  - **Dependencies:** Task 7
  - **Acceptance:** Filtering by campus/category returns correct subsets; room availability accurate

- [ ] **Task 10: Web — Calendar Page (Month View)**
  - Install and configure @fullcalendar/react
  - Build CalendarPage with month/week/day/list view toggle
  - Integrate campus + category filter dropdowns
  - Implement color-coded event rendering by category
  - Build "today" highlight with liturgical season color
  - Implement "+N more" overflow for dense days
  - Connect to GET /events API with date range params
  - Style with Tailwind matching mockup design
  - **Dependencies:** Task 7, Task 9
  - **Acceptance:** Calendar renders August 2026 matching mockup; filters work; view switching < 800ms

- [ ] **Task 11: Web — Event Detail Flyout**
  - Build slide-in panel component (420px, right-side, with overlay)
  - Display event metadata (title, date, time, location, recurrence)
  - Display category badge with color
  - Display description section
  - Build amenities chip grid (Livestream, Accessible, Childcare, Parking)
  - Display scripture readings section (if assigned)
  - Display volunteer roster with status indicators
  - Build action buttons (Watch Livestream, Add to Calendar)
  - Show attendance progress bar
  - **Dependencies:** Task 10
  - **Acceptance:** Clicking event opens flyout matching mockup; all sections populated from API

- [ ] **Task 12: Web — Dashboard/Home Page**
  - Build liturgical season banner (auto-theme from current season)
  - Build Today's Scripture card (from /readings/today)
  - Build Upcoming Events list (from /events/upcoming?limit=4)
  - Build My Serving card (from /me/assignments)
  - Build Prayer Focus card (from /prayer-focuses/current)
  - Build Alerts/Action Needed card (from /volunteer/gaps + /notifications)
  - Implement responsive layout (two-column on desktop)
  - **Dependencies:** Task 7, Task 10
  - **Acceptance:** Dashboard loads in < 1.5s; all cards populated; liturgical theme applied

---

### Sprint 3: Registration & Volunteers (Weeks 5-6)

- [ ] **Task 13: RSVP & Registration API**
  - Implement POST /events/:id/register (with capacity check, auto-waitlist)
  - Implement PUT /events/:id/register (update party_size, status)
  - Implement DELETE /events/:id/register (cancel → trigger waitlist promotion)
  - Implement GET /events/:id/registrations (admin: full list with CSV export)
  - Implement waitlist auto-promotion (FIFO on cancellation)
  - Handle race condition for concurrent RSVPs (advisory lock or optimistic locking)
  - Prevent duplicate registration (REG_002 error)
  - **Dependencies:** Task 7
  - **Acceptance:** Capacity enforcement works; waitlist promotes correctly; concurrent RSVPs handled

- [ ] **Task 14: Volunteer Roles & Assignment API**
  - Implement CRUD for /volunteer/roles (min/max counts, org-scoped)
  - Implement POST /volunteer/assignments (assign member to role × event)
  - Implement PUT /volunteer/assignments/:id (confirm, decline, swap-request)
  - Implement GET /volunteer/gaps (unfilled roles for next 14 days)
  - Implement GET /volunteer/schedule?start=&end= (role × event matrix data)
  - Implement POST /volunteer/swap (create swap request between two members)
  - Implement PUT /volunteer/swap/:id (accept/decline swap)
  - **Dependencies:** Task 7
  - **Acceptance:** Gap detection works; swap lifecycle completes; matrix data correct

- [ ] **Task 15: Auto-Scheduler Engine**
  - Implement round-robin algorithm weighted by serving frequency
  - Respect member opt-out dates and max frequency preferences
  - Respect role qualifications (member must have role capability)
  - Return draft assignments for admin review
  - Report unfillable gaps with reason
  - **Dependencies:** Task 14
  - **Acceptance:** Auto-schedule fills gaps fairly; respects constraints; reports unfillable roles

- [ ] **Task 16: Web — Volunteer Schedule View**
  - Build role × event matrix grid component
  - Implement status pills (confirmed green, pending yellow, open orange dashed)
  - Build gap alert banner with count and "View Gaps" link
  - Implement "Send Reminders" button (batch notification trigger)
  - Implement "Auto-Schedule" button (triggers engine, shows draft)
  - Build Export button (CSV download)
  - Build grid/list view toggle
  - **Dependencies:** Task 14, Task 15
  - **Acceptance:** Matrix renders matching mockup; status indicators correct; auto-schedule flow works

- [ ] **Task 17: Web — Event Creation Wizard**
  - Build 4-step modal component with progress bar
  - Step 1: Basics (title, category, type, description, cover image upload)
  - Step 2: When/Where (date pickers, recurrence config, campus, room with availability)
  - Step 3: Options (RSVP toggle, capacity, childcare, livestream, visibility, contact, tags)
  - Step 4: Review & Publish (summary card, Save Draft / Publish buttons)
  - Implement form state persistence across steps (Zustand)
  - Implement room conflict check on Step 2 completion
  - Implement image upload to S3 (presigned URL)
  - **Dependencies:** Task 7, Task 9
  - **Acceptance:** Full wizard flow creates event; room conflicts detected; drafts saveable

- [ ] **Task 18: Web — Left Sidebar Navigation**
  - Build persistent sidebar component (240px, dark navy)
  - Implement navigation items with active state highlighting
  - Build user profile section (avatar, name, role badge)
  - Implement notification bell with unread count
  - Implement search icon with command palette (⌘K)
  - Build responsive collapse for smaller viewports
  - **Dependencies:** Task 10, Task 12
  - **Acceptance:** All nav items route correctly; active state matches current page; responsive behavior works

---

### Sprint 4: Liturgical & Scripture (Weeks 7-8)

- [ ] **Task 19: Liturgical Season API & Theming**
  - Implement GET /liturgical/current (returns active season with color)
  - Implement GET /liturgical/seasons?year=2026 (full year list)
  - Implement POST /liturgical/generate (auto-compute dates by denomination)
  - Implement Easter date calculation (Computus algorithm)
  - Build ThemeProvider (web) that reads current season and sets CSS variables
  - Build 8 season color palettes (Ordinary, Advent, Christmas, Epiphany, Lent, Holy Week, Easter, Pentecost)
  - **Dependencies:** Task 4
  - **Acceptance:** Current season returns correct data; theme changes per season; Easter calc accurate for 2026-2030

- [ ] **Task 20: Scripture & Lectionary API**
  - Implement CRUD for /readings (reading_type, reference, reading_date)
  - Implement GET /readings/today (by org, current date)
  - Implement POST /readings/bulk-import (parse lectionary file, create 208+ records)
  - Support RCL, Catholic, and Lutheran lectionary formats
  - Validate scripture references (book + chapter:verse format)
  - **Dependencies:** Task 4
  - **Acceptance:** Bulk import creates correct readings; today's reading returns accurately

- [ ] **Task 21: Prayer Focus & Ministry Groups API**
  - Implement CRUD for /prayer-focuses (weekly/monthly/special types)
  - Implement GET /prayer-focuses/current (active by date range)
  - Implement CRUD for /groups (name, type, meeting_day, campus, leader)
  - Implement /groups/:id/members (add, remove, list with roles)
  - Auto-generate recurring events for groups with meeting schedules
  - **Dependencies:** Task 7
  - **Acceptance:** Prayer focus returns current; group events auto-created; member management works

- [ ] **Task 22: Calendar Sync & Export**
  - Implement GET /calendar/ical (authenticated iCal feed URL)
  - Generate proper .ics format with VEVENT, RRULE, VTIMEZONE
  - Implement "Add to Calendar" button (generate .ics download)
  - Implement Google Calendar deep link generation
  - Implement Outlook deep link generation
  - Include location, description, organizer in exports
  - **Dependencies:** Task 8
  - **Acceptance:** iCal feed imports correctly into Google Calendar and Apple Calendar

- [ ] **Task 23: Global Search API**
  - Implement GET /search?q= (PostgreSQL full-text search)
  - Search across events (title, description), groups (name), members (name), readings (reference)
  - Return results grouped by type with relevance ranking
  - Implement search suggestions / autocomplete
  - Build search UI with command palette (⌘K shortcut)
  - **Dependencies:** Task 7, Task 21
  - **Acceptance:** Search returns relevant results across all entity types; < 200ms response

---

### Sprint 5: Mobile App (Weeks 9-10)

- [ ] **Task 24: Mobile — Navigation & Shell**
  - Configure React Navigation (Tab + Stack navigators)
  - Build 5-tab bottom nav (Home, Calendar, FAB, Serve, More)
  - Build center FAB with quick-add bottom sheet
  - Configure deep link handling (roca:// URI scheme)
  - Configure status bar and safe area handling
  - Build shared header component with back nav and actions
  - **Dependencies:** Task 1
  - **Acceptance:** Tab navigation works; deep links route correctly; FAB opens action sheet

- [ ] **Task 25: Mobile — Home Screen**
  - Build liturgical banner with season color theming
  - Build scripture card with today's reading
  - Build upcoming events list (scrollable, colored left borders)
  - Build My Serving section with status chips
  - Build prayer focus card
  - Implement pull-to-refresh
  - Connect to API (events/upcoming, readings/today, prayer-focuses/current, me/assignments)
  - **Dependencies:** Task 24, Task 12 (API endpoints)
  - **Acceptance:** Home screen matches mockup; data loads < 2s; pull-to-refresh works

- [ ] **Task 26: Mobile — Calendar Screen**
  - Build month view calendar grid with dot indicators
  - Implement view mode toggle (Month, Week, Day, List)
  - Build day detail section below grid (event list for selected day)
  - Implement swipe gestures (left/right for month navigation)
  - Build long-press quick-add for date selection
  - Implement campus/category filter sheet
  - **Dependencies:** Task 24, Task 8
  - **Acceptance:** Calendar renders correctly; swipe navigation smooth (60fps); filters work

- [ ] **Task 27: Mobile — Event Detail Screen**
  - Build full event detail view (metadata, description, amenities, readings)
  - Build attendance progress bar
  - Build action buttons (Watch Livestream, Add to Calendar)
  - Implement RSVP inline (register/cancel buttons)
  - Implement share action (native share sheet)
  - **Dependencies:** Task 24, Task 11
  - **Acceptance:** Event detail displays all sections; RSVP works; share generates correct link

- [ ] **Task 28: Mobile — Serve Screens (3 tabs)**
  - Build segment control (My Schedule / Openings / Swaps)
  - My Schedule: assignment cards with confirm/decline/swap buttons
  - Openings: gap cards grouped by role with "Sign Me Up" action
  - Swaps: incoming/outgoing swap requests with accept/decline
  - Implement stats section (serving count, streak)
  - **Dependencies:** Task 24, Task 14
  - **Acceptance:** All three tabs match mockups; confirm/decline/swap flows complete

- [ ] **Task 29: Mobile — Event Creation Wizard**
  - Build 4-step mobile wizard with progress indicator
  - Step 1: Title, category, type, description, image picker
  - Step 2: Date/time pickers (native), campus, room
  - Step 3: Toggles (RSVP, childcare, livestream), visibility, contact, tags
  - Step 4: Review summary + Publish/Save Draft
  - Implement keyboard avoidance and scroll management
  - **Dependencies:** Task 24, Task 7
  - **Acceptance:** Full creation flow works on iOS + Android; form validation correct

- [ ] **Task 30: Mobile — Offline Mode**
  - Configure WatermelonDB schema mirroring key tables
  - Implement sync adapter (pull server changes, push local queue)
  - Cache events (next 7 days), assignments, groups, readings
  - Implement offline indicator banner
  - Queue writes (RSVP, confirm/decline, swap requests) when offline
  - Implement conflict resolution on reconnect
  - Handle 24-hour staleness warning
  - **Dependencies:** Task 25, Task 26, Task 28
  - **Acceptance:** App functions offline; queued actions sync on reconnect; conflicts resolved correctly

---

### Sprint 6: Notifications & Polish (Weeks 11-12)

- [ ] **Task 31: Push Notification System**
  - Configure APNs (iOS) credentials and entitlements
  - Configure FCM (Android) project and service account
  - Implement notification creation API (POST /notifications)
  - Implement SQS-based dispatch queue with retry logic
  - Build 9 notification type handlers with payload templates
  - Implement deep link generation per notification type
  - Implement notification center API (GET /notifications, PUT /read, /read-all)
  - Implement quiet hours configuration
  - **Dependencies:** Task 4, Task 24
  - **Acceptance:** Push notifications deliver on iOS + Android < 5s; deep links resolve; quiet hours respected

- [ ] **Task 32: Mobile — Notifications Screen**
  - Build notification center with tab filters (All, Events, Serve, Alerts)
  - Implement read/unread states with visual indicators
  - Implement swipe-to-dismiss and mark-all-read
  - Display notification grouping by time period (Today, Yesterday, Earlier)
  - **Dependencies:** Task 31, Task 24
  - **Acceptance:** Notifications display correctly; read state persists; filters work

- [ ] **Task 33: Mobile — Groups Screen**
  - Build My Groups list with color-coded cards
  - Display meeting times, member count, leader info
  - Build group detail view (roster, upcoming events, description)
  - Implement "Browse All Groups" discovery view
  - **Dependencies:** Task 24, Task 21
  - **Acceptance:** Groups display correctly; navigation to detail works; roster shows members

- [ ] **Task 34: Analytics Dashboard**
  - Build admin analytics page (attendance trends chart)
  - Implement weekly/monthly attendance comparison
  - Build volunteer metrics (participation rate, gap rate, top volunteers)
  - Build RSVP conversion funnel (registered → attended)
  - Build member engagement score
  - Implement date range selection and export
  - **Dependencies:** Task 13, Task 14
  - **Acceptance:** Charts render with real data; date range filtering works; export generates CSV/PDF

- [ ] **Task 35: Performance Optimization**
  - Implement React Query cache strategies (staleTime, gcTime)
  - Add database query optimization (EXPLAIN ANALYZE on slow queries)
  - Implement API response compression (gzip/brotli)
  - Add CDN cache headers for static assets
  - Implement mobile list virtualization (FlashList)
  - Optimize calendar rendering for 200+ events/month
  - Run Lighthouse audit and fix LCP/CLS issues
  - **Dependencies:** All previous tasks
  - **Acceptance:** All performance targets met (API p95 < 300ms, LCP < 1.5s, mobile start < 2s)

- [ ] **Task 36: Accessibility Audit & Fixes**
  - Run axe-core automated scan on all pages
  - Fix all critical/serious WCAG 2.1 AA violations
  - Implement keyboard navigation for all modals, wizards, and forms
  - Add ARIA labels to all interactive components
  - Test with VoiceOver (iOS) and TalkBack (Android)
  - Verify color contrast across all liturgical season themes
  - Test Dynamic Type scaling (iOS) and font size accessibility (Android)
  - **Dependencies:** All UI tasks
  - **Acceptance:** Zero critical/serious axe violations; full keyboard navigation; screen reader compatible

- [ ] **Task 37: Security Hardening**
  - Run OWASP ZAP scan against staging API
  - Fix any identified vulnerabilities
  - Verify RBAC boundary tests (7 roles × all endpoints)
  - Test multi-tenancy isolation (cross-org data leakage)
  - Verify rate limiting per tier
  - Run SQL injection fuzzing on all input fields
  - Configure WAF rules
  - **Dependencies:** All API tasks
  - **Acceptance:** Zero high/critical findings; RBAC enforced on all endpoints; no cross-org leakage

- [ ] **Task 38: End-to-End Test Suite**
  - Write Playwright E2E tests for critical flows:
    - Member onboarding → dashboard view
    - Event creation → calendar display → event detail
    - RSVP → capacity check → waitlist → promotion
    - Volunteer assignment → confirm → swap lifecycle
    - Event cancellation → notification cascade
    - Calendar export → iCal validation
  - Write Detox E2E tests for mobile critical flows
  - Configure E2E in CI pipeline (staging environment)
  - **Dependencies:** All previous tasks
  - **Acceptance:** All E2E tests pass on staging; CI pipeline runs them on every deploy

- [ ] **Task 39: Documentation & Handoff**
  - Generate OpenAPI 3.0 spec from route definitions
  - Create Postman/Bruno collection with all endpoints
  - Write API documentation with examples
  - Document deployment runbook
  - Document database backup and restore procedures
  - Create onboarding guide for new developers
  - **Dependencies:** All tasks
  - **Acceptance:** API docs deployed; runbook covers all ops scenarios; new dev can onboard in < 1 day

